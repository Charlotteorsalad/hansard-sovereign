.venv/
__pycache__/
data/
# keep the UI design reference tables (the CLI in tools/ui-design needs them)
!tools/ui-design/data/
*.egg-info/

# Python build and cache artifacts
*.pyc
.ruff_cache/

# Prebuilt-index tarball (published to a GitHub Release, not committed)
hansard-data.tar.gz

# IDE / tool settings
.vscode/
.claude/

# Fine-tuned model artifacts (safetensors/gguf, large — not committed)
hansard-qwen-gguf/
*.gguf
hansard-qwen-gguf.zip

# fine-tune intermediate (train.jsonl + val.jsonl are the committed split)
finetune/examples.jsonl
