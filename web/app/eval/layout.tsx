export const metadata = {
  title: "Inference Benchmark — Hansard Sovereign",
  description:
    "Run a live inference benchmark against the local LLM and watch TTFT, tokens/sec and VRAM update in real time.",
};

export default function EvalLayout({ children }: { children: React.ReactNode }) {
  return children;
}
